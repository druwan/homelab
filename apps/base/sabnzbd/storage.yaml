apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sabnzbd-config-pvc
spec:
  resources:
    requests:
      storage: 2Gi
  storageClassName: truenas-pvc-nfs
  accessModes:
    - ReadWriteOnce
