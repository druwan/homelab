apiVersion: v1
kind: Service
metadata:
  labels:
    app: bento-pdf
  name: bento-pdf
spec:
  type: ClusterIP
  selector:
    app: bento-pdf
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
