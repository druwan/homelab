apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mealie-data-pvc
  namespace: mealie
spec:
  storageClassName: truenas-pvc-nfs
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
