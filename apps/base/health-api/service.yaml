apiVersion: v1
kind: Service
metadata:
  labels:
    app: health-api
  name: health-api
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8000
      targetPort: http
  selector:
    app: health-api
