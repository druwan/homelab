apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: radarr-config-pvc
  namespace: media
spec:
  resources:
    requests:
      storage: 10Gi
  storageClassName: truenas-pvc-nfs
  accessModes:
    - ReadWriteOnce
