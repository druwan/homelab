apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: radarr
  name: radarr
spec:
  replicas: 1
  selector:
    matchLabels:
      app: radarr
  template:
    metadata:
      labels:
        app: radarr
    spec:
      securityContext:
        fsGroup: 3100
        supplementalGroups: [3100]
      containers:
        - name: radarr
          image: ghcr.io/linuxserver/radarr:6.3.0.10514-ls313
          ports:
            - containerPort: 7878
          securityContext:
            allowPrivilegeEscalation: false
          env:
            - name: PUID
              value: "1000"
            - name: PGID
              value: "3100"
            - name: TZ
              value: Europe/Stockholm
          volumeMounts:
            - mountPath: /config
              name: radarr-config
            - mountPath: /media
              name: media
          resources:
            requests:
              memory: 384Mi
            limits:
              memory: 768Mi
      volumes:
        - name: radarr-config
          persistentVolumeClaim:
            claimName: radarr-config-pvc-v2
        - name: media
          persistentVolumeClaim:
            claimName: media-pvc
