apiVersion: v1
kind: Service
metadata:
  name: firefly
  namespace: firefly
  labels:
    app: firefly
spec:
  selector:
    app: firefly
  ports:
    - name: http
      port: 80
      targetPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: firefly-importer
  namespace: firefly
  labels:
    app: firefly-importer
spec:
  selector:
    app: firefly-importer
  ports:
    - name: http
      port: 80
      targetPort: 8080
