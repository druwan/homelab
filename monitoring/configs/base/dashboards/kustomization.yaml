apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: monitoring
configMapGenerator:
  - name: cnpg-dashboards
    files:
      # https://github.com/cloudnative-pg/grafana-dashboards
      - cnpg/cnpg-grafana-dashboard.json
    options:
      labels:
        grafana-dashboard: "1"
