apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: renovate-secret
  namespace: renovate
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: azure-kv-store
  target:
    name: renovate-bot-secret
    creationPolicy: Owner
    template:
      data:
        RENOVATE_TOKEN: "{{ .RENOVATE_TOKEN }}"
        RENOVATE_HOST_RULES: '[{"matchHost":"ghcr.io","hostType":"docker","token":"{{ .RENOVATE_TOKEN }}","concurrentRequestLimit":4,"maxRequestsPerSecond":4}]'
  data:
    - secretKey: RENOVATE_TOKEN
      remoteRef:
        key: renovate-token
