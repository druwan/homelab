apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: n8n-db
  namespace: n8n
spec:
  selector:
    matchLabels:
      cnpg.io/cluster: n8n-db-v2
  podMetricsEndpoints:
    - port: metrics
