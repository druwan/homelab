apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: kaneo-db
  namespace: kaneo
spec:
  selector:
    matchLabels:
      cnpg.io/cluster: kaneo-db-v1
  podMetricsEndpoints:
    - port: metrics
