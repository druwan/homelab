apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: linkwarden-db
  namespace: linkwarden
spec:
  selector:
    matchLabels:
      cnpg.io/cluster: linkwarden-db-v2
  podMetricsEndpoints:
    - port: metrics
