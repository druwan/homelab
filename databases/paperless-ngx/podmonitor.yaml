apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: paperless-ngx-db
  namespace: paperless-ngx
spec:
  selector:
    matchLabels:
      cnpg.io/cluster: paperless-ngx-db-v2
  podMetricsEndpoints:
    - port: metrics
