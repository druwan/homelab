apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: sure-db
  namespace: sure
spec:
  selector:
    matchLabels:
      cnpg.io/cluster: sure-db-v0
  podMetricsEndpoints:
    - port: metrics
