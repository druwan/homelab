apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: mealie-db
  namespace: mealie
spec:
  selector:
    matchLabels:
      cnpg.io/cluster: mealie-db-v2
  podMetricsEndpoints:
    - port: metrics
