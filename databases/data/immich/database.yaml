apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: immich-db
  namespace: immich
spec:
  description: Postgres cluster for immich app
  imageName: ghcr.io/cloudnative-pg/postgis:17-3-standard-trixie
  imagePullPolicy: Always
  inheritedMetadata:
    labels:
      app: immich
      policy-type: database
  bootstrap:
    initdb:
      postInitSQL:
        - CREATE EXTENSION IF NOT EXISTS vector CASCADE;
  instances: 3
  monitoring:
    enablePodMonitor: true
  plugins:
    - name: barman-cloud.cloudnative-pg.io
      isWALArchiver: true
      parameters:
        barmanObjectName: immich-azure-store
  storage:
    size: 100Gi
