apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: authentik-db
  namespace: authentik
spec:
  selector:
    matchLabels:
      cnpg.io/cluster: authentik-db-v1
  podMetricsEndpoints:
    - port: metrics
